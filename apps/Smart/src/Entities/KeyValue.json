{
  "name": "KeyValue",
  "type": "object",
  "properties": {
    "scope": {
      "type": "string",
      "description": "Un \u00e1mbito o espacio de nombres para el par clave-valor, utilizado para filtrar."
    },
    "value_json": {
      "type": "object",
      "additionalProperties": true,
      "description": "Un objeto JSON para almacenar el valor."
    }
  },
  "required": [
    "scope"
  ]
}
