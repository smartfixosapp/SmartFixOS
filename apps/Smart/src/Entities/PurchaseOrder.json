{
  "name": "PurchaseOrder",
  "type": "object",
  "description": "\u00d3rdenes de compra a proveedores con l\u00edneas de items",
  "properties": {
    "po_number": {
      "type": "string",
      "description": "N\u00famero de orden de compra (auto-generado)"
    },
    "supplier_id": {
      "type": "string",
      "description": "ID del proveedor"
    },
    "supplier_name": {
      "type": "string",
      "description": "Nombre del proveedor (cache)"
    },
    "status": {
      "type": "string",
      "enum": [
        "draft",
        "ordered",
        "received",
        "cancelled"
      ],
      "default": "draft",
      "description": "Estado de la orden"
    },
    "order_date": {
      "type": "string",
      "format": "date",
      "description": "Fecha de la orden"
    },
    "expected_date": {
      "type": "string",
      "format": "date",
      "description": "Fecha esperada de entrega"
    },
    "received_date": {
      "type": "string",
      "format": "date",
      "description": "Fecha real de recepci\u00f3n"
    },
    "line_items": {
      "type": "array",
      "description": "L\u00edneas de items de la orden",
      "items": {
        "type": "object",
        "properties": {
          "id": {
            "type": "string"
          },
          "inventory_item_id": {
            "type": "string",
            "description": "ID del item de inventario"
          },
          "product_name": {
            "type": "string",
            "description": "Nombre del producto (cache)"
          },
          "description": {
            "type": "string",
            "description": "Descripci\u00f3n del item"
          },
          "quantity": {
            "type": "number",
            "minimum": 0,
            "description": "Cantidad ordenada"
          },
          "unit_cost": {
            "type": "number",
            "minimum": 0,
            "description": "Costo unitario"
          },
          "tax_rate": {
            "type": "number",
            "default": 0,
            "description": "Tasa de impuesto"
          },
          "tax_amount": {
            "type": "number",
            "default": 0,
            "description": "Monto de impuesto"
          },
          "line_total": {
            "type": "number",
            "minimum": 0,
            "description": "Total de la l\u00ednea (qty * unit_cost + tax)"
          },
          "linked_work_order_id": {
            "type": "string",
            "description": "ID de Work Order asociado (opcional)"
          },
          "linked_work_order_number": {
            "type": "string",
            "description": "N\u00famero de WO (cache)"
          },
          "external_link": {
            "type": "string",
            "description": "URL del producto en el sitio del proveedor"
          }
        }
      }
    },
    "subtotal": {
      "type": "number",
      "default": 0,
      "minimum": 0,
      "description": "Subtotal sin impuestos"
    },
    "tax_amount": {
      "type": "number",
      "default": 0,
      "minimum": 0,
      "description": "Monto total de impuestos"
    },
    "shipping_cost": {
      "type": "number",
      "default": 0,
      "minimum": 0,
      "description": "Costo de env\u00edo"
    },
    "total_amount": {
      "type": "number",
      "default": 0,
      "minimum": 0,
      "description": "Total de la orden"
    },
    "currency": {
      "type": "string",
      "default": "USD",
      "description": "Moneda"
    },
    "notes": {
      "type": "string",
      "description": "Notas de la orden"
    },
    "tracking_number": {
      "type": "string",
      "description": "N\u00famero de seguimiento"
    },
    "created_by": {
      "type": "string",
      "description": "Usuario que cre\u00f3 la orden"
    },
    "created_by_name": {
      "type": "string",
      "description": "Nombre del usuario que cre\u00f3"
    },
    "received_by": {
      "type": "string",
      "description": "Usuario que recibi\u00f3 la orden"
    },
    "received_by_name": {
      "type": "string",
      "description": "Nombre del usuario que recibi\u00f3"
    }
  },
  "required": [
    "po_number",
    "supplier_id",
    "order_date"
  ]
}
